# global variable for manifests
@manifest_fulfilled_value = 0.75
# Germany 1/2
faction_manifest_expansionism_and_rearment = {
	# TODO find goal names on localization later
	name = expansionism_and_rearment_goal_name
	description = expansionism_and_rearment_goal_desc
	is_manifest = yes
	ratio_progress = {
		# TODO check collections
		total_amount_collection = collection:german_potential_core_states
		completed_amount_collection = collection:germany_owned_core_states
		range = {
			max = 1
		}
		scale = {
			industrial_capacity_factory = 0.15
			political_power_factor = 0.1
		}
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = {
					experience_gain_army_unit_factor = 0.2
				}
			}
		}
	}
}

# Germany 2/2
faction_manifest_lebensraum = {
	# TODO find goal names on localization later
	name = lebensraum_goal_name
	description = lebensraum_goal_desc
	is_manifest = yes
	ratio_progress = {
		# TODO check collections
		total_amount_collection = collection:germany_lebensraum_objectives
		completed_amount_collection = collection:germany_lebensraum_objectives_controlled
		range = {
			max = 1
		}
		scale = {
			conscription = 0.02
			offensive_war_stability_factor = 0.2
			weekly_casualties_war_support = 0.005
		}
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = {
					army_core_attack_factor = 0.3
					army_core_defence_factor = 0.3
				}
			}
		}
	}
}

# UK 1/2
faction_manifest_appeasement = {
	# TODO find goal names on localization later
	name = appeasement_goal_name
	description = appeasement_goal_desc
	is_manifest = yes
	ratio_progress = {
		# TODO check collections
		total_amount_collection = collection:prevent_germany_potential_claim_states
		completed_amount_collection = collection:gave_away_germany_owned_states
		range = {
			max = 1
		}
		scale = {
			line_change_production_efficiency_factor = 0.2
			stability_factor = 0.1
		}
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = {
					political_power_factor = 0.2
				}
			}
		}
	}
}

# UK 2/2
faction_manifest_defend_empire = {
	# TODO find goal names on localization later
	name = defend_empire_goal_name
	description = defend_empire_goal_desc
	is_manifest = yes
	ratio_progress = {
		# TODO check collections
		total_amount_collection = collection:defend_empire_members_states
		completed_amount_collection = collection:lost_empire_members_states
		range = {
			max = 1
		}
		scale = {
			production_factory_efficiency_gain_factor = 0.2
			navy_max_range_factor = 0.25
		}
		progress_sections = {
			manifest_fulfilled = {
				min = 0.95
				modifier = {
					army_core_defence_factor = 0.2
				}
			}
		}
	}
}

# SOVIETS 1/2
faction_manifest_rush_industralization = {
	# TODO find goal names on localization later
	name = rush_industralization_goal_name
	description = rush_industralization_goal_desc
	is_manifest = yes
	ratio_progress = {
		# TODO check collections
		total_amount_collection = collection:germany_lebensraum_objectives
		completed_amount_collection = collection:germany_lebensraum_objectives_controlled
		range = {
			max = 1
		}
		scale = {
			weekly_manpower = -5000
			production_speed_industrial_complex_factor = 0.2
			production_speed_infrastructure_factor = 0.2
		}
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = {
					research_speed_factor = 0.2
				}
			}
		}
	}
}

# SOVIETS 2/2
faction_manifest_patriotic_war = {
	# TODO find goal names on localization later
	name = patriotic_war_goal_name
	description = patriotic_war_goal_desc
	is_manifest = yes
	ratio_progress = {
		# TODO check collections
		total_amount_collection = collection:germany_lebensraum_objectives
		completed_amount_collection = collection:germany_lebensraum_objectives_controlled
		range = {
			max = 1
		}
		scale = {
			command_abilities_cost_factor = -0.5
			supply_node_range = 0.2
		}
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = {
					army_core_defence_factor = 0.1
				}
			}
		}
	}
}

# JAPAN 1/2
faction_manifest_conquest_and_supremacy = {
	# TODO find goal names on localization later
	name = conquest_and_supremacy_goal_name
	description = conquest_and_supremacy_goal_desc
	is_manifest = yes
	ratio_progress = {
		# TODO check collections
		total_amount_collection = collection:germany_lebensraum_objectives
		completed_amount_collection = collection:germany_lebensraum_objectives_controlled
		range = {
			max = 1
		}
		scale = {
			political_power_factor = 0.1
			production_lack_of_resource_penalty_factor = -0.3
		}
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = {
					production_speed_buildings_factor = 0.2					#after french indochina
				}
			}
		}
	}
}

# JAPAN 2/2
faction_manifest_nanshinron = {
	# TODO find goal names on localization later
	name = nanshinron_goal_name
	description = nanshinron_goal_desc
	is_manifest = yes
	ratio_progress = {
		# TODO check collections
		total_amount_collection = collection:germany_lebensraum_objectives
		completed_amount_collection = collection:germany_lebensraum_objectives_controlled
		range = {
			max = 1
		}
		scale = {
			industrial_capacity_dockyard = 0.2
			air_range_factor = 0.3
		}
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = {
					navy_max_range_factor = 0.3
				}
			}
		}
	}
}
